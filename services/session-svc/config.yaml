server:
  port: 8083
  jwt_secret: "changeme-in-production"

mysql:
  dsn: "root:123456@tcp(127.0.0.1:3306)/im_session?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 3

log:
  json: false

kafka:
  brokers:
    - "localhost:9092"
  topic_message_push: "message_push"
  topic_session_sync: "session_sync"
  consumer_group_id: "session-svc"

prometheus:
  enabled: true
  path: /metrics
