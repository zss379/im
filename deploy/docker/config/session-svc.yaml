server:
  port: 8083
  jwt_secret: "dev-secret"

mysql:
  dsn: "root:root123@tcp(mysql:3306)/im_session?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  addr: "redis:6379"
  password: "redis123"
  db: 3

log:
  json: true

kafka:
  brokers:
    - "kafka:9092"
  topic_message_push: "message_push"
  topic_session_sync: "session_sync"
  consumer_group_id: "session-svc"

prometheus:
  enabled: true
  path: /metrics
