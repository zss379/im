server:
  port: 8088
  jwt_secret: "dev-secret"

mysql:
  dsn: "root:root123@tcp(mysql:3306)/im_shared?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  addr: "redis:6379"
  password: "redis123"
  db: 0

log:
  level: "debug"
  json: true

prometheus:
  enabled: true
  path: "/metrics"
